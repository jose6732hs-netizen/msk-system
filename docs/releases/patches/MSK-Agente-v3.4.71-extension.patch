--- background-direct-agent.js
+++ background-direct-agent.js
@@ transport routing
+ const runCommandText = String(payload?.original_command || payload?.command || payload?.message || '');
+ const runHasAttachments = Array.isArray(payload?.attachments) && payload.attachments.length > 0;
+ const runHasLongDocumentation = action === 'run' && runCommandText.length > 9000 && /(?:documenta[cç][aã]o|endpoint|headers?|payload|webhook|\b(?:GET|POST|PUT|PATCH|DELETE)\s+\/)/i.test(runCommandText);
- action === 'run' ? 'msk-agent-fast' : 'msk-agent-public'
+ action === 'run' ? ((runHasAttachments || runHasLongDocumentation) ? 'msk-agent-public' : 'msk-agent-fast') : 'msk-agent-public'
- FAST 90000 / COMPLEX 165000
+ FAST 120000 / COMPLEX 240000
+ const safeTransportRetry = ['preflight','health','status','task-status','vault-status','vault-list'].includes(action);
+ const maxTransportAttempts = safeTransportRetry ? 3 : 1;
+ backoff: 1000ms, 3000ms
+ RUN em HTTP 408/499/502/503/504 => EXTENSION_RESPONSE_LOST para reconciliação por task_id; nunca retry cego do RUN.

--- msk-direct-agent.js
+++ msk-direct-agent.js
@@ transport reconciliation
- 24 polls
+ 84 polls (~149s com backoff adaptativo)
+ terminal inclui awaiting_credentials
+ anexos textuais são redigidos com redactSecretsForAgent antes do envio
+ credenciais explícitas em formato key=value também ativam o Credential Vault

--- supabase/functions/msk-agent-public/index.ts
+++ supabase/functions/msk-agent-public/index.ts
+ valida SHA-256 e device-authorize do corpo original antes de transformar a mensagem
+ anexos e documentação >9000 chars passam por msk-attachment-analyze
+ documentação longa vira contexto técnico compacto <= 11600 chars para o editor atual
+ remove history, active_skill e reinforced_command antes de encaminhar a edição
+ encaminhamento interno preserva Authorization, apikey, sessão e licença, sem reutilizar assinatura de um corpo transformado
