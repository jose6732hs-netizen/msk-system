/**
 * O aviso global de contingência do WhatsApp foi removido da interface.
 * Mantemos o componente vazio por compatibilidade com o root existente,
 * sem interferir na navegação mobile/desktop.
 */
export function WhatsappSupportButton() {
  return null;
}
